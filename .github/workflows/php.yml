name: PHP 8.4 Kontrol

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
      - run: find upload/src/addons/WarextStudios/ThreadFreshness -name '*.php' -print0 | xargs -0 -n1 php -l
      - run: php tests/status_calculator.php
